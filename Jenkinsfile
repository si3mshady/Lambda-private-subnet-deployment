pipeline {
    agent any

    stages {
       
        stage('run ansible playbook') {
            steps {
                echo 'ansible-playbook ansible/launch_pub_priv_vpc_nat.yml'
            }
        }
      
    }
}